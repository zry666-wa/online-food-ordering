<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.food.dao.OrderItemMapper">

    <resultMap id="itemMap" type="OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="dishId" column="dish_id"/>
        <result property="dishName" column="dish_name"/>
        <result property="price" column="price"/>
        <result property="quantity" column="quantity"/>
        <result property="subtotal" column="subtotal"/>
    </resultMap>

    <select id="findByOrderId" resultMap="itemMap">
        SELECT * FROM t_order_item WHERE order_id = #{orderId}
    </select>

    <insert id="insert" parameterType="OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_order_item (order_id, dish_id, dish_name, price, quantity, subtotal)
        VALUES (#{orderId}, #{dishId}, #{dishName}, #{price}, #{quantity}, #{subtotal})
    </insert>

    <insert id="insertBatch">
        INSERT INTO t_order_item (order_id, dish_id, dish_name, price, quantity, subtotal) VALUES
        <foreach collection="list" item="it" separator=",">
            (#{it.orderId}, #{it.dishId}, #{it.dishName}, #{it.price}, #{it.quantity}, #{it.subtotal})
        </foreach>
    </insert>
</mapper>
