<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.food.dao.OrderMapper">

    <resultMap id="orderMap" type="Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="address" column="address"/>
        <result property="phone" column="phone"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="payTime" column="pay_time"/>
        <result property="finishTime" column="finish_time"/>
        <result property="username" column="username"/>
        <result property="shopName" column="shop_name"/>
        <collection property="items" ofType="OrderItem">
            <id property="id" column="item_id"/>
            <result property="orderId" column="item_order_id"/>
            <result property="dishId" column="item_dish_id"/>
            <result property="dishName" column="item_dish_name"/>
            <result property="price" column="item_price"/>
            <result property="quantity" column="item_quantity"/>
            <result property="subtotal" column="item_subtotal"/>
        </collection>
    </resultMap>

    <sql id="baseSelect">
        SELECT o.*, u.username, m.shop_name
        FROM t_order o
        LEFT JOIN t_user u ON o.user_id = u.id
        LEFT JOIN t_merchant m ON o.merchant_id = m.id
    </sql>

    <select id="findById" resultMap="orderMap">
        <include refid="baseSelect"/>
        WHERE o.id = #{id}
    </select>

    <select id="findByOrderNo" resultMap="orderMap">
        <include refid="baseSelect"/>
        WHERE o.order_no = #{orderNo}
    </select>

    <select id="findByUserId" resultMap="orderMap">
        <include refid="baseSelect"/>
        WHERE o.user_id = #{userId}
        ORDER BY o.id DESC
    </select>

    <select id="findByMerchantId" resultMap="orderMap">
        <include refid="baseSelect"/>
        WHERE o.merchant_id = #{merchantId}
        <if test="status != null">AND o.status = #{status}</if>
        ORDER BY o.id DESC
    </select>

    <select id="findAll" resultMap="orderMap">
        <include refid="baseSelect"/>
        <where>
            <if test="status != null">AND o.status = #{status}</if>
            <if test="keyword != null and keyword != ''">
                AND (o.order_no LIKE CONCAT('%',#{keyword},'%') OR u.username LIKE CONCAT('%',#{keyword},'%') OR m.shop_name LIKE CONCAT('%',#{keyword},'%'))
            </if>
        </where>
        ORDER BY o.id DESC
    </select>

    <insert id="insert" parameterType="Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_order (order_no, user_id, merchant_id, total_amount, status, address, phone, remark)
        VALUES (#{orderNo}, #{userId}, #{merchantId}, #{totalAmount}, #{status}, #{address}, #{phone}, #{remark})
    </insert>

    <update id="update" parameterType="Order">
        UPDATE t_order
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="payTime != null">pay_time = #{payTime},</if>
            <if test="finishTime != null">finish_time = #{finishTime},</if>
            <if test="address != null">address = #{address},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE t_order SET status = #{status}
        <choose>
            <when test="status == 4">, finish_time = NOW()</when>
            <when test="status == 1">, pay_time = NOW()</when>
        </choose>
        WHERE id = #{id}
    </update>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM t_order WHERE status = #{status}
    </select>

    <select id="sumAmount" resultType="java.math.BigDecimal">
        SELECT IFNULL(SUM(total_amount),0) FROM t_order WHERE status IN (1,2,3,4)
    </select>
</mapper>
